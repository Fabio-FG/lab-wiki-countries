import { Navbar as Nav } from 'react-bootstrap';

function Navbar() {
  return (
    <Nav bg="primary" expand="lg">
      <a class="navbar-brand" href="/">
        WikiCountries
      </a>
    </Nav>
  );
}

export default Navbar;
